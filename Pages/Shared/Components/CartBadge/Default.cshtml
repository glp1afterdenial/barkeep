@model int

<a class="nav-link" asp-page="/Cart/Index">
    <i class="bi bi-cart3 me-1"></i>Cart
    @if (Model > 0)
    {
        <span class="badge bg-warning rounded-pill">@Model</span>
    }
</a>
